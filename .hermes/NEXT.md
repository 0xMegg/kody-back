# Kody Backend NEXT

Current mode: backend Hermes active.

Next action:
- Preserve pre-existing dirty state unless the user explicitly asks to clean or commit it.
- For backend work, read `kody-backend-conventions.md` and relevant source files before editing.

Open gates:
- Prisma schema/migration, env, lockfile, dependency, or API contract changes require explicit approval.
